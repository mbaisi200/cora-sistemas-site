# Instruções para o Assistente de Código

1. Nunca fazer commit antes de ser solicitado pelo usuário.
2. Sempre que fizer uma alteração no código, iniciar servidor com live reload para atualizar o browser automaticamente. Usar: `npx browser-sync . --watch --no-open`
